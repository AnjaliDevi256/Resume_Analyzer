{
  "name": "resume-analyzer-fullstack",
  "version": "1.0.0",
  "description": "Full-stack Resume Analyzer deployed on a single Render web service.",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "build:render": "npm install && npm install --prefix frontend && npm run build --prefix frontend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "express",
    "react",
    "mongodb",
    "render"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "fs": "0.0.1-security",
    "mongoose": "^8.4.1",
    "multer": "^1.4.5-lts.1",
    "pdf-parse": "^1.1.1",
    "path": "^0.12.7"
  }
}